#!/bin/sh

# Prefered programs
export EDITOR=nvim
export VISUAL="$EDITOR"
export TERMINAL=urxvtc
export PAGER=less
export BROWSER=firefox-developer-edition
export MANPAGER='less -R --use-color -Dd+R -DuG' # Colorful man page
export SUDO_EDITOR=/usr/bin/"$EDITOR" # I don't use sudo(1), but specify anyway
export DOAS_EDITOR="$SUDO_EDITOR" # This require by my doasedit()
export SXHKD_SHELL=/bin/sh # Use sh(1) as a shell
