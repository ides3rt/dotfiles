#
# Applications hotkeys
#

# Terminal
mod1 + Return
	alacritty

# Programs launcher
mod1 + p
	rofi -show drun -show-icons

# Spawn `firefox`
mod1 + w
	firefox

#
# System hotkeys
#

# Take a screenshot
Print
	scrot -q 100 '%Y-%m-%d_%H:%M:%S.png' -e 'mv $f ~/pix/'

# Advance screenshot options
shift + Print
	$HOME/ex/rofi/scrot

# Open my dictionary script
mod1 + d
	$HOME/ex/rofi/dictionary

# Increase volume
mod1 + equal
	pactl set-sink-volume @DEFAULT_SINK@ +5%

# Decrease volume
mod1 + minus
	pactl set-sink-volume @DEFAULT_SINK@ -5%

# Change keyboard layouts
mod4 + Escape
	$HOME/ex/sxhkd/loadkey

#
# BSPWM hotkeys
#

# Quit BSPWM
mod1 + shift + q
	bspc quit

# Restart BSPWM
mod1 + shift + r
	pkill -USR1 -x sxhkd; bspc wm -r

# Close focused window
mod1 + q
	bspc node -c

# Change between tiled/monocle layout
mod1 + m
	bspc desktop -l next

# Change focused window's state
mod1 + {t,f,c}
	bspc node -t {~tiled,~fullscreen,~floating}

# Change focused window's node to sticky
mod1 + s
	bspc node -g sticky

# Change focused window's node
# mod1 + {m,x,z}
# 	bspc node -g {marked,locked,private}

# Focus the node in the given direction
mod1 + {_,shift + }{n,e,i,o}
	bspc node -{f,s} {west,south,north,east}

# Focus or send to the given desktop
mod1 + {_,shift + }{1-9,0}
	bspc {desktop -f,node -d} '^{1-9,10}'

# Preselect the direction
mod1 + ctrl + {n,e,i,o}
	bspc node -p {west,south,north,east}

# Preselect the ratio
mod1 + ctrl + {1-9}
	bspc node -o 0.{1-9}

# Cancel the preselection for the focused node
mod1 + ctrl + w
	bspc node -p cancel

# Move a floating window
mod4 + shift + {n,e,i,o}
	bspc node -v {-100 0,0 100,0 -100,100 0}

# Grow focused window's width
mod4 + n
	bspc node -z left -50 0; bspc node -z right 50 0

# Shrink focused window's height
mod4 + i
	bspc node -z bottom 0 50; bspc node -z top 0 -50

# Grow focused window's height
mod4 + e
	bspc node -z bottom 0 -50; bspc node -z top 0 50

# Shrink focused window's width
mod4 + o
	bspc node -z left 50 0; bspc node -z right -50 0
