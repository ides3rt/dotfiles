#!/usr/bin/env bash

up() {
	if [ -n $1 ]; then
		local Number=$1
		while [ $Number -gt 0 ]; do
			cd .. && let Number--
		done
	else
		cd ..
	fi
}

lq() {
	links -http.do-not-track 1 \
		-http.referer 0 \
		-save-url-history 0 \
		-save-cookies 0 https://lite.duckduckgo.com/lite/
}
